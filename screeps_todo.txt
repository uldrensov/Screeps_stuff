BUGS:
	phase archit prematurely ends

TODO frontlog:
	room 0 has unnecessary ramparts (consider how acolytes function if tx/rx breaks)
	redraw room 0's extension layout
	relieve dependence on SD by adding find() calls to units instead of passing ID args
	optimise role parts
	kill edrone when he is no longer needed
		-requires detection of drones/assims/available energy
	dynamically choose drone size when its time to replace it (middle gnd between waiting for edrone and trying to spawn a big one)
	reduce dependence on repetitive find() calls (globalise check interval? currently 10)
	purifiers should contest cores that are taken (e.g. controller subducts and core is still not dead)
	stop remote workers from spawning if the ctrl belongs to invader (currently coded but not working; orbassim line 41)
	[DEFENCE] think about how tower should behave in general
	[SOFTLOCK] before calling a softlock, drones should deposit pocket energy if there are no more assims/energy on the map
	[SOFTLOCK] take into acc the drone's ignore lim and their current pocket energy when calcing room energy?
	war probe + drone = doesnt go beyond walls; only bare essentials for base maintenance
		//war mode: disables pickups and tombs for those who can see it
	
TODO backlog:
	[QOL] more status report info (remote room info, wall/rampart distinction, invader/core alert)
	modify treasurer to work w labs/factories too
	treasurer withdraw/deposit RESOURCES_ALL
	dont let new things spawn when one briefly leaves room (expand everything as crossroom in unitdrive?)
	work-part / thief detection in remotes?
	[MAINTENANCE] decouple hardcodes from unitdrive (and from argpasses?)
	[MAINTENANCE] energy source / nexus / ctrller vars could be merged
	[MAINTENANCE] git init (put this file inside), readme
	[MAINTENANCE] trek to standby vs. rally at flag
	[QOL] localnexi energy count distinction in statusreport
	
Look up:
	-

JS help:
	lodash
	filter method (lambda notation)
	prototypes
	utils